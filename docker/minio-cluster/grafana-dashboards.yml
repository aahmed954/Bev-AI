apiVersion: 1

providers:
  - name: 'MinIO Dashboards'
    orgId: 1
    folder: 'MinIO'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards